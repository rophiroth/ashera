import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'org.psyhackers.Ashera',
    appName: 'Ashera',
    webDir: 'out',
    server: {
        androidScheme: 'https'
    }
};

export default config;
